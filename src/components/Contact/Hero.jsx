// eslint-disable-next-line no-unused-vars
import React from "react";

/**
 * Hero component.
 * Displays the hero section of the contact page with a heading.
 */
const Hero = () => {
  return (
    <section className="section">
      {/* Hero heading */}
      <h1 className="hero-heading">Let&apos;s Connect!</h1>
    </section>
  );
};

export default Hero;
